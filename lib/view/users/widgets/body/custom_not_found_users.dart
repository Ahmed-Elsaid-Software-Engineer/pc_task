import 'package:flutter/material.dart';

class CustomNotFoundUsers extends StatelessWidget {
  const CustomNotFoundUsers({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("not Found any users"),
    );
  }
}
